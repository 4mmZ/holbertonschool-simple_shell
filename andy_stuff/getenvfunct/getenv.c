#include "main.h"

extern char ** environ;
  
char * get_env(char *name)
{
	/*/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin*/
	char 
	char *path = "";
	return (path);
}
void main(void)
{
	printf("pete\n");
	printf("%s\n", get_env("PATH"));
}
